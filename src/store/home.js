const state = {
  homeInfo: {},
};

const mutations = {
  RECEIVE_HOME_INFO(state, value) {
    state.homeInfo = value;
  },
};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
