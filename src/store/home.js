const state = {
  homeInfo: {},
};

const mutations = {
  RECEIVE_HOMEINFO(state, value) {
    state.homeInfo = value;
  },
};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters,
};
